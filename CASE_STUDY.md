# Кейс: Разработка сайта ремонта стиральных машин

## О проекте

**Клиент**: Частный мастер по ремонту стиральных машин (Санкт-Петербург)
**Задача**: Создать лендинг для привлечения клиентов на ремонт стиральных машин
**Стек**: Next.js 16, React 19, TypeScript, Tailwind CSS 4
**Тип**: Статический сайт (SSG) для быстрой загрузки и SEO

---

## Реализованный функционал

### 1. Система брендов (51 бренд)

**Функционал:**
- Динамические страницы для каждого бренда (`/[brand]/page.tsx`)
- Страница со всеми брендами (`/brands/page.tsx`)
- Логотипы с адаптивным масштабированием
- Кнопка "Показать все бренды" для экономии места

**Проблемы и решения:**

| Проблема | Решение |
|----------|---------|
| Логотипы разных размеров не помещались в карточки | Добавили параметр `scale` для каждого бренда в `brands.ts` |
| Kuppersberg и Schaub-Lorenz не влезали даже с масштабом | Убрали масштабирование, оставили overflow-hidden на контейнере |
| 51 бренд занимал слишком много места на главной | Создали `BrandsSection.tsx` с показом топ-12 на мобилках, топ-15 на ПК |
| При hover на логотип курсор "прыгал" | Добавили `pointer-events-none` на Image |

**Ключевой код (BrandsSection.tsx):**
```tsx
const getVisibilityClass = (index: number) => {
  if (showAll) return "";
  if (index >= 15) return "hidden";
  else if (index >= 12) return "hidden sm:flex";
  return "";
};
```

---

### 2. Форма консультации с маской телефона

**Функционал:**
- Российская маска +7 (XXX) XXX-XX-XX
- Автоматическое позиционирование курсора
- Валидация номера перед отправкой
- Отправка заявок в Telegram

**Проблемы и решения:**

| Проблема | Решение |
|----------|---------|
| Курсор перескакивал в конец поля при вводе | Используем `requestAnimationFrame` для корректной установки позиции |
| Backspace удалял символы маски вместо цифр | Кастомный обработчик `onKeyDown` с `e.preventDefault()` |
| Клик по полю ставил курсор в неправильное место | Отдельные обработчики `onFocus` и `onClick` |

**Ключевой код (ConsultationForm.tsx):**
```tsx
function formatPhoneNumber(value: string): string {
  const digits = value.replace(/\D/g, "");
  let phone = digits;
  if (phone.startsWith("8") || phone.startsWith("7")) {
    phone = phone.slice(1);
  }
  phone = phone.slice(0, 10).padEnd(10, "_");
  return `+7 (${phone.slice(0, 3)}) ${phone.slice(3, 6)}-${phone.slice(6, 8)}-${phone.slice(8)}`;
}

function getCursorPosition(value: string): number {
  const underscoreIndex = value.indexOf("_");
  return underscoreIndex === -1 ? value.length : underscoreIndex;
}
```

---

### 3. Блок цен с категориями

**Функционал:**
- 3 категории: Неисправности, Замена деталей, Услуги
- 18 карточек услуг с иконками
- Hover-dropdown с детализацией цен (desktop)
- Popup-форма при клике на карточку
- Контекст бренда передается в форму

**Проблемы и решения:**

| Проблема | Решение |
|----------|---------|
| Иконки нужны были кастомные, но без библиотек | Создали SVG-компонент `ProblemIcon` с 15+ иконками |
| На мобилках hover не работает | Убрали hover-dropdown на мобилках, только popup по клику |
| Dropdown обрезался за границей секции | Добавили `z-50` и правильное позиционирование |
| Скролл страницы при открытом popup | `document.body.style.overflow = "hidden"` в useEffect |

**Ключевой код (PricingSection.tsx):**
```tsx
useEffect(() => {
  if (isOpen) {
    document.body.style.overflow = "hidden";
  } else {
    document.body.style.overflow = "";
  }
  return () => {
    document.body.style.overflow = "";
  };
}, [isOpen]);
```

---

### 4. Интеграция с Telegram

**Функционал:**
- Отправка заявок через Telegram Bot API
- Форматирование сообщений с emoji
- Указание источника заявки (форма консультации / блок цен)
- Московское время в сообщениях

**Проблемы и решения:**

| Проблема | Решение |
|----------|---------|
| Токен бота светился в репозитории | Перенесли в `.env.local` (уже в .gitignore) |
| Нужен был разный формат для разных форм | Добавили параметр `formSource` в функцию |

**Ключевой код (telegram.ts):**
```tsx
const message = `
🔧 *Новая заявка с сайта*
━━━━━━━━━━━━━━━
📋 *Источник:* ${formSource}
${data.problem ? `❓ *Проблема:* ${data.problem}` : ""}
${data.name ? `👤 *Имя:* ${data.name}` : ""}
${data.phone ? `📞 *Телефон:* ${data.phone}` : ""}
${data.brand ? `🏭 *Марка:* ${data.brand}` : ""}
${data.address ? `📍 *Адрес:* ${data.address}` : ""}
━━━━━━━━━━━━━━━
🕐 ${new Date().toLocaleString("ru-RU", { timeZone: "Europe/Moscow" })}
`.trim();
```

---

### 5. Хедер с dropdown меню

**Функционал:**
- Sticky хедер при скролле
- Dropdown со всеми 51 брендом (6 колонок)
- Мобильное burger-меню
- Блокировка скролла при открытом меню

**Проблемы и решения:**

| Проблема | Решение |
|----------|---------|
| Dropdown с 51 брендом был узким | Расширили до 700px с 6 колонками |
| При открытом burger-меню страница скроллилась | `document.body.style.overflow = "hidden"` |
| Dropdown закрывался при движении мыши к нему | Добавили `pt-2` (padding-top) для зазора |

**Ключевой код (Header.tsx):**
```tsx
<div className="w-[700px] bg-white rounded-lg shadow-lg border border-gray-100 p-4">
  <div className="grid grid-cols-6 gap-x-4 gap-y-1 text-sm">
    {brands.map((brand) => (
      <Link key={brand.slug} href={`/${brand.slug}`} className="truncate">
        {brand.name}
      </Link>
    ))}
  </div>
</div>
```

---

### 6. SEO-оптимизация

**Функционал:**
- Schema.org разметка (LocalBusiness + Service)
- Динамические meta-теги для каждого бренда
- Sitemap и robots.txt
- Статическая генерация всех страниц

**Реализация (JsonLd.tsx):**
```tsx
const jsonLd = {
  "@context": "https://schema.org",
  "@type": "HomeAndConstructionBusiness",
  name: `Ремонт стиральных машин - ${siteConfig.masterName}`,
  telephone: siteConfig.phone,
  openingHoursSpecification: {
    dayOfWeek: ["Monday", "Tuesday", ...],
    opens: "08:00",
    closes: "21:00",
  },
};
```

---

### 7. Карточки отзывов

**Функционал:**
- Данные из Profi.ru (рейтинг 4.96, 131+ отзыв)
- Сворачивание длинных отзывов (>280 символов)
- Аватар с первой буквой имени
- Бейдж "Profi.ru" для доверия

**Ключевой код (ReviewCard.tsx):**
```tsx
const needsTruncation = review.text.length > 280;

<p className="text-gray-600 text-sm">
  {isExpanded || !needsTruncation
    ? review.text
    : `${review.text.substring(0, 280)}...`}
</p>

{needsTruncation && (
  <button onClick={() => setIsExpanded(!isExpanded)}>
    {isExpanded ? "Свернуть" : "Читать полностью"}
  </button>
)}
```

---

## Структура проекта

```
src/
├── app/
│   ├── page.tsx           # Главная страница
│   ├── brands/page.tsx    # Страница всех брендов
│   ├── [brand]/page.tsx   # Динамические страницы брендов
│   ├── layout.tsx         # Корневой layout
│   ├── sitemap.ts         # Генерация sitemap
│   └── robots.ts          # robots.txt
├── components/
│   ├── Header.tsx         # Хедер с навигацией
│   ├── Footer.tsx         # Футер
│   ├── ConsultationForm.tsx   # Форма консультации
│   ├── PricingSection.tsx     # Блок цен с popup
│   ├── BrandsSection.tsx      # Секция брендов
│   ├── ReviewCard.tsx         # Карточка отзыва
│   ├── JsonLd.tsx             # SEO-разметка
│   └── SmoothScroll.tsx       # Плавный скролл
├── data/
│   ├── brands.ts          # 51 бренд с логотипами
│   ├── prices.ts          # Цены и категории
│   ├── reviews.ts         # Отзывы
│   ├── reviews.json       # Данные отзывов
│   └── site.ts            # Конфигурация сайта
└── utils/
    └── telegram.ts        # Telegram Bot API
```

---

## Технические решения

### Статическая генерация (SSG)
```tsx
// next.config.ts
output: "export",
basePath: "/washing-master",
trailingSlash: true,
```

### Генерация страниц брендов
```tsx
export async function generateStaticParams() {
  return getAllBrandSlugs().map((brand) => ({ brand }));
}
```

### Адаптивная сетка
```tsx
// Мобилка: 2 колонки, Планшет: 3, ПК: 5
<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-4">
```

---

## Итоги

**Что реализовано:**
- Лендинг с 51 страницей брендов
- Формы сбора заявок с Telegram-интеграцией
- Полная SEO-оптимизация
- Адаптивный дизайн (mobile-first)
- Статическая сборка для быстрой загрузки

**Ключевые метрики:**
- 51 бренд с индивидуальными страницами
- 18 карточек услуг с детализацией цен
- 8+ отзывов с Profi.ru
- Рейтинг мастера: 4.96 из 5

**Деплой:** Статический экспорт на поддомен `/washing-master/`
